android {
            defaultConfig {
                applicationId = "org.nativescript.nggram"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }